sudo pip install kivy
sudo pip install kivymd

##to set size
size_hint = (.2, .05) # size_hint = (width, height) 0-1

size = (300, 100), size_hint = (None, None) #to set static size

##to change position
pos_hint={'center_x':0.5, 'center_y':0.5} #(0-1)

pos = (50, 500) # pos=(Left to Right, Bottom to Top)

##to set background-color
background_color=(255,255,255,.5) #rgb (0-255), alpha(0-1)

##to set font-size
font_size=100
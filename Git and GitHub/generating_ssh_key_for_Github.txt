ls ~/.ssh: to check 'ssh' file.
(id_rsa: to system)
(id_rsa.pub: to github)

ssh-keygen -t rsa -b 4096 -C 'artificialworld09@gmail.com': to generate ssh key.

cd ~/.ssh: to goto '.ssh' directory.

eval "$(ssh-agent -s)": to check 'ssh-agent' is live or not.

ssh-add -K ~/.ssh/id_rsa: to add ssh to system.

cat ~/.ssh/id_rsa.pub: copy and goto 'github.com/settings/SSH Key and GPG Key/New SSH key/ add SSH key:-
Title: Window10
Key: (past key)
ADD KEY

Goto: github.com/repository/(select repo)/copy ssh url.

git remote -v: to check saved git repo.

git remote remove my_origin: to delete repo.

git remote add this_origin "ssh url": to add repo.

git -T git@github.com: to check ssh url add or not.